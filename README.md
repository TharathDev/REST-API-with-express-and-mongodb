# REST-API-with-express-and-mongodb

## INSTALLATION!

```bash
npm install
```

Create `.env` file under the project directory:

```bash
touch .env
```

Populate `.env` as follows:

```bash
PORT=XXXX
MONGODB_URL=
DB_NAME=
DB_USER=
DB_PASS=
```

## START SERVICE

```bash
npm start
```

